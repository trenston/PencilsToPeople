import 'package:flutter/material.dart';

class GetInvolvedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Get Involved', style: TextStyle(fontSize: 24)),
    );
  }
}

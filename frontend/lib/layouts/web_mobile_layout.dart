import 'package:flutter/material.dart';

class WebMobileLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Web Mobile Layout'));
  }
}

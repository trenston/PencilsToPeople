import 'package:flutter/material.dart';

class WebDesktopLayout extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Web Desktop Layout'));
  }
}
